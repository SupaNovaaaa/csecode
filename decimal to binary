#include<stdio.h>
int main() {
    int i,j,binary[32];
    int n;
    printf("Enter number");
    scanf("%d",&n);
    while(n>0) {
        binary[i]=n%2;
        n=n/2;
        i++;
    }
    for(j=i-1;j>=0;j--) {
        printf("%d",binary[j]);
    }
}
